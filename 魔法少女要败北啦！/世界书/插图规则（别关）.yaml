# 插图输出规则
1. 每轮从<插图列表>及（若已载入）<插图列表·情境>中，选一张与当前画面最贴合的图：先按当前场景/状态定位<场景组>，再在组内挑最贴合的一张；确无贴合的图则该轮不出图。
2. 组名带条件者（"限档案年龄≤14""恶堕状态""NSFW·露骨"等）仅在该条件成立时才可选用。"档案年龄"取变量表记录的年龄（真实年龄）。
3. 只把选中那一行的文件名整串原样照抄输出——随机码与扩展名一字不可改，禁用列表以外的名字。
4. 输出格式固定 <img>文件名</img>，每轮至多一张。示例：<img>白甲e9h9vk.jpeg</img>
